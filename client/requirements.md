## Packages
framer-motion | Page transitions and detailed interaction animations
recharts | Visualizing mood trends and goal progress
date-fns | Advanced date formatting for diary entries and timelines
react-day-picker | Calendar component for the timeline view
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
Authentication uses Replit Auth (backend routes already set up).
Frontend should handle 401s by redirecting to /api/login (handled in use-auth).
API hooks should use the Zod schemas from @shared/routes.
Theme: Minimalist, serif headings (Playfair Display), clean sans-serif body (Inter/DM Sans).
